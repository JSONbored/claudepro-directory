# Legacy Data Archive

These CSV exports came from the pre-HeyClaude application model and are not read by the current registry, web app, or validation scripts.

Current active data under `content/data/` is limited to generated audit output and legacy vote seed inputs used by the D1 vote migration path.
