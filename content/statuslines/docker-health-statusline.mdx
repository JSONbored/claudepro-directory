---
title: Claude Code Docker Statusline - Container Health Monitoring
slug: docker-health-statusline
category: statuslines
description: >-
  Docker statusline configuration for Claude Code CLI. Features real-time health
  monitoring, color-coded indicators, and container tracking. Production-ready.
cardDescription: >-
  Docker statusline configuration for Claude Code CLI. Features real-time health
  monitoring, color-coded indicators, and container tracking.
seoTitle: Claude Code Docker Statusline - Container Health Monitoring
seoDescription: >-
  Docker statusline configuration for Claude Code CLI. Features real-time health
  monitoring, color-coded indicators, and container tracking. Production-ready.
author: JSONbored
authorProfileUrl: 'https://github.com/JSONbored'
dateAdded: '2025-10-19'
repoUrl: 'https://github.com/JSONbored/claudepro-directory'
documentationUrl: 'https://docs.docker.com/reference/cli/docker/container/stats/'
installable: true
copySnippet: "#!/bin/bash\n\n# Docker Health Statusline\n# Real-time container health monitoring for Claude Code CLI\n\n# Get running containers count\nrunning=$(docker ps -q 2>/dev/null | wc -l | tr -d ' ')\n\n# Get total containers\ntotal=$(docker ps -a -q 2>/dev/null | wc -l | tr -d ' ')\n\n# Get unhealthy containers\nunhealthy=$(docker ps --filter health=unhealthy -q 2>/dev/null | wc -l | tr -d ' ')\n\n# Color codes\nGREEN='\\033[0;32m'\nYELLOW='\\033[1;33m'\nRED='\\033[0;31m'\nBLUE='\\033[0;34m'\nNC='\\033[0m' # No Color\n\n# Status indicator\nif [ \"$unhealthy\" -gt 0 ]; then\n    status=\"${RED}●${NC}\"\nelif [ \"$running\" -eq 0 ]; then\n    status=\"${YELLOW}○${NC}\"\nelse\n    status=\"${GREEN}●${NC}\"\nfi\n\n# Display statusline\necho -e \"${BLUE}\U0001F433${NC} ${status} ${running}/${total}\""
scriptLanguage: bash
scriptBody: "#!/bin/bash\n\n# Docker Health Statusline\n# Real-time container health monitoring for Claude Code CLI\n\n# Get running containers count\nrunning=$(docker ps -q 2>/dev/null | wc -l | tr -d ' ')\n\n# Get total containers\ntotal=$(docker ps -a -q 2>/dev/null | wc -l | tr -d ' ')\n\n# Get unhealthy containers\nunhealthy=$(docker ps --filter health=unhealthy -q 2>/dev/null | wc -l | tr -d ' ')\n\n# Color codes\nGREEN='\\033[0;32m'\nYELLOW='\\033[1;33m'\nRED='\\033[0;31m'\nBLUE='\\033[0;34m'\nNC='\\033[0m' # No Color\n\n# Status indicator\nif [ \"$unhealthy\" -gt 0 ]; then\n    status=\"${RED}●${NC}\"\nelif [ \"$running\" -eq 0 ]; then\n    status=\"${YELLOW}○${NC}\"\nelse\n    status=\"${GREEN}●${NC}\"\nfi\n\n# Display statusline\necho -e \"${BLUE}\U0001F433${NC} ${status} ${running}/${total}\""
tags:
  - docker
  - statusline
  - monitoring
  - health-check
  - containers
  - CLI
  - claude-code
  - devops
keywords:
  - claude
  - claude-code
  - CLI
  - containers
  - development
  - devops
  - docker
  - health
  - health-check
  - monitoring
  - statusline
  - statuslines
  - tools
readingTime: 1
viewCount: 8
copyCount: 0
popularityScore: 13
difficultyScore: 2
hasTroubleshooting: true
hasPrerequisites: false
hasBreakingChanges: false
robotsIndex: true
robotsFollow: true
---
```bash
#!/bin/bash

# Docker Health Statusline
# Real-time container health monitoring for Claude Code CLI

# Get running containers count
running=$(docker ps -q 2>/dev/null | wc -l | tr -d ' ')

# Get total containers
total=$(docker ps -a -q 2>/dev/null | wc -l | tr -d ' ')

# Get unhealthy containers
unhealthy=$(docker ps --filter health=unhealthy -q 2>/dev/null | wc -l | tr -d ' ')

# Color codes
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# Status indicator
if [ "$unhealthy" -gt 0 ]; then
    status="${RED}●${NC}"
elif [ "$running" -eq 0 ]; then
    status="${YELLOW}○${NC}"
else
    status="${GREEN}●${NC}"
fi

# Display statusline
echo -e "${BLUE}🐳${NC} ${status} ${running}/${total}"
```
