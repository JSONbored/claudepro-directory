# pnpm Configuration
# This file configures pnpm behavior for the claudepro-directory project

# ==============================================================================
# Peer Dependency Handling
# ==============================================================================
# We're using Next.js 16 canary for advanced build optimizations (70%+ faster builds).
# Some dependencies haven't updated their peer dependency ranges yet, but they're
# fully compatible with Next.js 16 (it's backwards compatible with 14 and 15).
#
# Related packages with "invalid" peer deps (all working fine):
# - @arcjet/next (security/WAF)
# - @nosecone/next (security headers)
# - @storybook/nextjs (component dev)
# - next-safe-action (type-safe server actions)
# - @vercel/analytics, @vercel/speed-insights
#
# This will be removed when Next.js 16 becomes stable and packages update their
# peer dependency ranges (estimated: January 2026).
# ==============================================================================
auto-install-peers=true
strict-peer-dependencies=false

# ==============================================================================
# Performance: Faster installs with symlinks
# ==============================================================================
# pnpm uses hard links by default, which is already optimal
# No additional configuration needed

# ==============================================================================
# Hoisting Configuration
# ==============================================================================
# Use shamefully-hoist for maximum compatibility with tools expecting npm/yarn
# This makes pnpm behave more like npm (all deps hoisted to node_modules root)
shamefully-hoist=true

# ==============================================================================
# Lockfile Configuration
# ==============================================================================
# Merge git conflict markers in lockfile automatically
merge-git-branch-lockfiles=true
