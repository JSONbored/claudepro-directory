/**
 * Submission Form Configuration Constants
 * Separate file to avoid "use server" export restrictions
 */

export const FORM_FIELDS_CACHE_TAG = 'submission-form-fields';
export const FORM_FIELDS_CACHE_SECONDS = 60 * 60 * 6; // 6 hours
