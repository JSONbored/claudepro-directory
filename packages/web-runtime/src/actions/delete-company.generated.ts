'use server';

// -----------------------------------------------------------------------------
// 🔒 AUTO-GENERATED - DO NOT EDIT
// Generated by packages/generators/src/commands/generate-server-actions.ts
// -----------------------------------------------------------------------------

import type { DeleteCompanyReturns } from '@heyclaude/database-types/postgres-types';
import { z } from 'zod';
import { authedAction } from './safe-action';
import { runRpc } from './run-rpc-instance';

const deleteCompanySchema = z.object({
  company_id: z.string().uuid()
});

export const deleteCompany = authedAction
  .metadata({ actionName: 'deleteCompany', category: 'content' })
  .inputSchema(deleteCompanySchema)
  .action(async ({ parsedInput, ctx }) => {
    try {
      const rawResult = await runRpc<DeleteCompanyReturns>(
        'delete_company',
        {
          'p_company_id': parsedInput.company_id,
          'p_user_id': ctx.userId
        },
        {
          action: 'deleteCompany.rpc',
          userId: ctx.userId,
        }
      );

      
      const result = rawResult;
      
      
      // Lazy import server-only dependencies
      const { revalidatePath, revalidateTag } = await import('next/cache');

      // Simple success check?
      // Some RPCs return void, some return { success: boolean }?
      // We assume implicit success if no error thrown by runRpc.
      
      revalidatePath(`/companies`);
      revalidatePath(`/account/companies`);
      revalidateTag(`company-${parsedInput.company_id}`, 'default');
      revalidateTag(`company-id-${parsedInput.company_id}`, 'default');
      revalidateTag(`companies`, 'default');

      

      return result;
    } catch (error) {
      // Lazy import logActionFailure
      const { logActionFailure } = await import('../errors');
      throw logActionFailure('deleteCompany', error, {
        userId: ctx.userId,
        input: parsedInput
      });
    }
  });
