/**
 * PostgreSQL Composite Type: toggle_job_status_result
 * 
 * 🔒 AUTO-GENERATED - DO NOT EDIT
 * Generated by prisma-postgres-types-generator
 */

import { z } from 'zod';

/**
 * PostgreSQL composite type: toggle_job_status_result
 */
export type ToggleJobStatusResult = {
  /** Attribute: success (bool, nullable) */
  success: boolean | null;
  /** Attribute: job_id (uuid, nullable) */
  job_id: string | null;
  /** Attribute: old_status (job_status, nullable) */
  old_status: 'draft' | 'pending_payment' | 'pending_review' | 'active' | 'expired' | 'rejected' | 'deleted' | null;
  /** Attribute: new_status (job_status, nullable) */
  new_status: 'draft' | 'pending_payment' | 'pending_review' | 'active' | 'expired' | 'rejected' | 'deleted' | null;
  /** Attribute: message (text, nullable) */
  message: string | null;
};

/**
 * Zod schema for toggle_job_status_result composite type
 */
export const toggleJobStatusResultSchema = z.object({
  /** Attribute: success */
  success: z.boolean().nullable(),
  /** Attribute: job_id */
  job_id: z.string().uuid().nullable(),
  /** Attribute: old_status */
  old_status: z.enum(['draft', 'pending_payment', 'pending_review', 'active', 'expired', 'rejected', 'deleted']).nullable(),
  /** Attribute: new_status */
  new_status: z.enum(['draft', 'pending_payment', 'pending_review', 'active', 'expired', 'rejected', 'deleted']).nullable(),
  /** Attribute: message */
  message: z.string().nullable(),
});

/**
 * Type inference from Zod schema (should match type above)
 */
export type ToggleJobStatusResultFromZod = z.infer<typeof toggleJobStatusResultSchema>;
