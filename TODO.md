# TODO

## Content and schema
- Continue enriching category-specific fields for `hooks`, `collections`, `mcp`, `skills`, `commands`, and `statuslines`.
- Reduce metadata-only pain points by improving structured fallbacks and extracting more usable snippets from existing content.
- Add stronger schema validation for category-specific fields once the current corpus is normalized.

## Cards and homepage
- Tighten preview card anatomy until it matches the `claw.tools` density more closely.
- Verify GitHub star links, docs links, and open actions across all cards.
- Polish hero section, live counter pills, search/filter presentation, and infinite scroll behavior.

## Detail pages
- Improve sticky sidebar hierarchy and source/download/install affordances.
- Keep code-heavy entries compact with better snippet expansion behavior.
- Tune category-aware section rendering for agents, rules, hooks, commands, skills, MCP, guides, collections, and statuslines.

## Site polish
- Refine navbar, footer, and wordmark/logo direction.
- Audit color usage against the `Caffeineish` token system so contrast stays clean across all surfaces.

## Deployment
- Last task: align the project fully with current OpenNext Cloudflare Workers docs, including local preview, asset handling, and deploy configuration.
