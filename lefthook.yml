# Lefthook Configuration - Claude Pro Directory
# Version: 3.0.0 (November 2025)
# Enhanced with auto-format, edge function type-check, and tech debt tracking

# ============================================================================
# OUTPUT CONTROL
# ============================================================================
output:
  - execution      # Show what's running
  - execution_out  # Show command output
  - skips         # Show skipped commands
  - failure       # Always show failures

# ============================================================================
# PRE-COMMIT: Fast checks on staged files only
# Runs in parallel for speed, fails fast on critical issues
# ============================================================================
pre-commit:
  parallel: true
  follow: true
  commands:
    # 1. Type checking (Next.js/Node code - build dirs only)
    type-check:
      glob: "**/*.{ts,tsx}"
      exclude: 
        - "apps/edge/functions/**"
        - "packages/edge-runtime/src/utils/email/**"
      run: |
        echo "🔍 Type checking build-critical code..."
        pnpm type-check:build
      fail_text: "❌ Type errors found in build-critical code. Fix them before committing."

    # 2. Type checking (Edge functions - Deno)
    type-check-edge:
      glob: "apps/edge/functions/**/*.ts"
      run: |
        echo "🦕 Type checking edge functions..."
        if command -v deno &> /dev/null; then
          set +e
          OUTPUT=$(pnpm type-check:edge 2>&1)
          STATUS=$?
          echo "$OUTPUT"
          if [ $STATUS -ne 0 ]; then
            if echo "$OUTPUT" | grep -F "packages/edge-runtime/src/utils/email/" >/dev/null; then
              echo "⚠️  Email template type errors detected but ignored (packages/edge-runtime/src/utils/email/**)."
              exit 0
            fi
            exit $STATUS
          fi
        else
          echo "⚠️  Deno not installed - skipping edge function type-check"
          echo "   Install: brew install deno"
        fi
      fail_text: "❌ Type errors found in edge functions. Fix them before committing."

    # 3. Lint and auto-fix with ESLint (build dirs only)
    lint:
      glob: "**/*.{ts,tsx}"
      exclude: 
        - "apps/edge/functions/**"
        - "packages/edge-runtime/src/utils/email/**"
      run: |
        echo "🔍 Linting build-critical code..."
        pnpm lint:build --fix
        git add -u
      fail_text: "❌ ESLint found errors in build-critical code. Fix them before committing."

    # 4. Validate Next.js config can be loaded (catches syntax errors ESLint/TS miss)
    validate-next-config:
      glob: "apps/web/next.config.mjs"
      run: |
        echo "🔍 Validating next.config.mjs syntax..."
        pnpm validate:next-config
      fail_text: "❌ next.config.mjs has syntax errors. Fix them before committing."

    # 5. Validate client/server boundaries (catches build-time import issues)
    validate-client-server:
      glob: "apps/web/src/**/*.{ts,tsx}"
      run: |
        echo "🔍 Validating client/server boundaries..."
        pnpm validate:client-server
      fail_text: "❌ Client/server boundary violations found. Fix them before committing."

    # 6. Run tests for changed files (non-blocking, warnings only)
    test-affected:
      glob: "**/*.{ts,tsx}"
      exclude: 
        - "apps/edge/functions/**"
        - "packages/edge-runtime/src/utils/email/**"
      run: |
        echo "🧪 Running tests for changed files (non-blocking)..."
        set +e
        # Run only tests related to changed files (vitest --changed)
        # This is fast and only runs tests that might be affected by changes
        pnpm test --run --changed 2>&1
        TEST_STATUS=$?
        if [ $TEST_STATUS -ne 0 ]; then
          echo "⚠️  Tests failed for changed files, trying all tests..."
          pnpm test --run 2>&1
          ALL_TEST_STATUS=$?
          if [ $ALL_TEST_STATUS -ne 0 ]; then
            echo "⚠️  Some tests failed (not blocking - fix before merging)"
            echo "   Run 'pnpm test' to see full output"
          else
            echo "✅ All tests passed"
          fi
        else
          echo "✅ Tests for changed files passed"
        fi
        exit 0
      fail_text: "⚠️  Tests failed (not blocking - fix before merging)."
      stage_fixed: false

# ============================================================================
# PRE-PUSH: Comprehensive checks before code reaches remote
# ============================================================================
pre-push:
  parallel: false
  commands:
    # 1. Type checking (build-critical code only)
    # Note: Only checks code that affects production build
    type-check-all:
      run: |
        echo "🔍 Type checking build-critical code..."
        set +e
        OUTPUT=$(pnpm type-check:build 2>&1)
        STATUS=$?
        echo "$OUTPUT"
        if [ $STATUS -ne 0 ]; then
          if echo "$OUTPUT" | grep -F "packages/edge-runtime/src/utils/email/" >/dev/null; then
            echo "⚠️  Email template type errors detected but ignored (packages/edge-runtime/src/utils/email/**)."
            exit 0
          fi
          exit $STATUS
        fi
      fail_text: "❌ Type errors found in build-critical code. Fix them before pushing."

    # 2. Production build verification (graceful)
    build:
      run: |
        echo "🏗️  Verifying production build..."
        if [ -z "$SUPABASE_URL" ] || [ -z "$NEXT_PUBLIC_SUPABASE_URL" ]; then
          echo "⚠️  Supabase env vars not set - skipping build verification"
          echo "   (CI will verify build with proper environment variables)"
          echo ""
          echo "   To run build locally, ensure these are set:"
          echo "   - SUPABASE_URL"
          echo "   - NEXT_PUBLIC_SUPABASE_URL"
          echo "   - SUPABASE_ANON_KEY"
          exit 0
        fi
        pnpm build
      fail_text: "❌ Build failed. Fix errors before pushing."

    # 3. Run full test suite (non-blocking, warnings only)
    test-all:
      run: |
        echo "🧪 Running full test suite (non-blocking)..."
        set +e
        pnpm test --run 2>&1
        TEST_STATUS=$?
        if [ $TEST_STATUS -ne 0 ]; then
          echo "⚠️  Some tests failed (not blocking - fix before merging)"
          echo "   Run 'pnpm test' to see full output"
        else
          echo "✅ All tests passed"
        fi
        exit 0
      fail_text: "⚠️  Tests failed (not blocking - fix before merging)."

    # 4. Technical debt analysis (non-blocking warnings)
    tech-debt:
      run: |
        echo "📊 Analyzing technical debt..."
        echo ""

        # Check for unused exports (knip)
        echo "🔍 Checking for unused exports..."
        if command -v npx &> /dev/null; then
          UNUSED=$(npx knip -c config/tools/knip.json --no-exit-code 2>/dev/null | grep -E "Unused (exports|types|dependencies)" | head -10 || true)
          if [ -n "$UNUSED" ]; then
            echo "$UNUSED"
            echo "   ... (run 'pnpm analyze:unused' for full report)"
          else
            echo "   ✅ No unused exports detected"
          fi
        else
          echo "   ⚠️  npx not available - skipping"
        fi

        echo ""

        # Check for circular dependencies
        echo "🔄 Checking for circular dependencies..."
          CIRCULAR=$(npx madge --circular --extensions ts,tsx apps/web/src/ 2>/dev/null || true)
        if [ -n "$CIRCULAR" ]; then
          echo "$CIRCULAR" | head -10
          echo "   ... (run 'pnpm analyze:circular' for full report)"
        else
          echo "   ✅ No circular dependencies detected"
        fi

        echo ""

        # Check for large files (>500 lines)
        echo "📏 Checking for large files (>500 LOC)..."
          LARGE_FILES=$(find apps/web/src apps/edge/functions -name '*.ts' -o -name '*.tsx' 2>/dev/null | while read file; do
          if [ -f "$file" ]; then
            lines=$(wc -l < "$file" 2>/dev/null || echo 0)
            if [ "$lines" -gt 500 ]; then
              echo "   ⚠️  $file ($lines lines) - consider splitting"
            fi
          fi
        done)

        if [ -n "$LARGE_FILES" ]; then
          echo "$LARGE_FILES" | head -10
        else
          echo "   ✅ No large files detected"
        fi

        echo ""
        echo "✅ Tech debt analysis complete (warnings only - not blocking)"
      fail_text: "ℹ️  Technical debt detected (not blocking - address iteratively)."

# ============================================================================
# COMMIT-MSG: Enforce conventional commits (with WIP support)
# ============================================================================
commit-msg:
  commands:
    conventional-commits:
      run: |
        MSG=$(cat {1})

        # Skip merge commits
        if echo "$MSG" | grep -qE "^Merge (branch|pull request)"; then
          exit 0
        fi

        # Skip fixup/squash commits
        if echo "$MSG" | grep -qE "^(fixup|squash)!"; then
          exit 0
        fi

        # Allow WIP commits with reduced validation
        if echo "$MSG" | grep -qE "^wip"; then
          echo "ℹ️  WIP commit detected - reduced validation"
          echo "   Remember to use proper conventional commit format when squashing/rebasing"
          exit 0
        fi

        # Validate conventional commit format
        if ! echo "$MSG" | grep -qE "^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\(.+\))?: .{1,}"; then
          echo "⚠️  Commit message doesn't follow Conventional Commits format"
          echo ""
          echo "Format: type(scope): description"
          echo "Types: feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert"
          echo ""
          echo "Examples:"
          echo "  feat(auth): add Google OAuth"
          echo "  fix(api): resolve race condition"
          echo "  wip: experimenting with new feature (skips validation)"
          echo ""
          echo "💡 Tip: Set LEFTHOOK_EXCLUDE=commit-msg to skip this check"
          exit 1
        fi

# ============================================================================
# POST-MERGE: Auto-install dependencies when package.json changes
# ============================================================================
post-merge:
  piped: true
  commands:
    deps-install:
      files: git diff-tree -r --name-only --no-commit-id ORIG_HEAD HEAD
      glob: "{package.json,pnpm-lock.yaml}"
      run: |
        echo "📦 Dependencies changed. Installing updates..."
        pnpm install
      fail_text: "⚠️  Dependency installation failed. Run 'pnpm install' manually."

# ============================================================================
# CONFIGURATION
# ============================================================================
colors: true
no_tty: false
min_version: 1.13.0

# ============================================================================
# USAGE TIPS
# ============================================================================
# Skip specific commands:
#   LEFTHOOK_EXCLUDE=type-check,lint git commit
#   LEFTHOOK_EXCLUDE=tech-debt git push
#
# Skip commit-msg validation:
#   LEFTHOOK_EXCLUDE=commit-msg git commit
#
# Skip build verification (when env vars missing):
#   LEFTHOOK_EXCLUDE=build git push
#
# Use WIP commits for rapid iteration:
#   git commit -m "wip: testing new approach"
#
# Verbose debugging:
#   LEFTHOOK_VERBOSE=1 git commit
#
# Skip all hooks (use sparingly):
#   git commit --no-verify
#   git push --no-verify
#
# Technical debt reports:
#   pnpm analyze:unused    # Full unused exports report
#   pnpm analyze:circular  # Full circular dependency report
# ============================================================================
