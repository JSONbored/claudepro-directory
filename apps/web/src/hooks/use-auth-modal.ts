'use client';

/**
 * useAuthModal Hook
 *
 * Re-export of useAuthModal from AuthModalProvider for convenience.
 * This allows importing the hook from a hooks directory for better organization.
 *
 * @module apps/web/src/hooks/use-auth-modal
 */

export { useAuthModal } from '@/src/components/core/auth/auth-modal-provider';
