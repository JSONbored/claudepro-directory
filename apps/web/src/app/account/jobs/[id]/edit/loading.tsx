'use client';

/**
 * Edit Job Loading Skeleton
 *
 * Loading skeleton for the edit job page.
 */

import { getLoadingComponent } from '@heyclaude/web-runtime/ui';

export default getLoadingComponent('/account/jobs/[id]/edit');
