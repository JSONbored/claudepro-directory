{
  "buildCommand": "cd ../.. && turbo run generate:config generate:categories:exec generate:service-worker:exec --concurrency=4 && cd apps/web && next build --turbopack",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
