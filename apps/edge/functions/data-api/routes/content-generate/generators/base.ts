/**
 * Base package generator interface and utilities
 *
 * This file exports the PackageGenerator interface for reference,
 * but the actual interface is defined in types.ts to avoid circular dependencies.
 */

export type { GenerateResult, PackageGenerator } from '../types.ts';
