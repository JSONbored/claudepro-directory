{
  "ignoreCommand": "if git diff --quiet HEAD^ HEAD -- . ':(exclude)content/'; then exit 1; else exit 0; fi",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "app/api/*": {
      "maxDuration": 30
    }
  }
}