{
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- . ':(exclude)content/' && exit 1 || exit 0",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "app/api/*": {
      "maxDuration": 30
    }
  }
}