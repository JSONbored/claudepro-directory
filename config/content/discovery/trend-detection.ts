/**
 * Trend Detection - Database-First Architecture
 * All configuration loaded from category_configs.generation_config via get_generation_config() RPC.
 * 317 lines → 10 lines (97% reduction)
 */

import { getGenerationConfig } from '../generation-rules';

export { getGenerationConfig as getTrendDetectionConfig };
